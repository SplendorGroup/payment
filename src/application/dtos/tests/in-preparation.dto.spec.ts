import { InPreparationDTO } from "../in-preparation.dto"

describe('InPreparationDTO', () => {
  it('should create a new instance of InPreparationDTO', () => {
    const dto = new InPreparationDTO();

    expect(dto).toBeDefined();
  });
});
